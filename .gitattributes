# --- Normalización de texto ---
* text=auto

# --- Archivos fuente del proyecto (Qt/C++) ---
*.cpp text diff=cpp
*.h   text diff=cpp
*.ui  text
*.pro text
CMakeLists.txt text

# --- Archivos SQL (base de datos) ---
*.sql text diff=sql

# --- Archivos de documentación ---
*.md  text
*.txt text
*.pdf binary
*.docx binary
*.xlsx binary

# --- Archivos binarios y base de datos ---
*.fdb binary
*.dll binary
*.exe binary
*.png binary
*.jpg binary
*.ico binary

# --- Ignorar diferencias automáticas en binarios ---
*.fdb -diff
*.dll -diff
*.exe -diff
*.png -diff
*.jpg -diff
*.ico -diff

# --- Git LFS (si lo activan en el futuro) ---
# *.zip filter=lfs diff=lfs merge=lfs -text
